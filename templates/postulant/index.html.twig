{% extends 'index.html.twig' %}

{% block title %}Vos annonces{% endblock %}

{% block body %}
    <h1>Vos annonces</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Recruteur</th>
                <th>Intitulé</th>
                <th>Lieu</th>
                <th>Description</th>
                <th>Valide</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for postulant in postulants %}
            <tr>
                <td>{{ postulant.id }}</td>
                <td>{{ postulant.getAnnonce[0].getRecruteur.getEntreprise }}</td>
                <td>{{ postulant.getAnnonce[0].intitule }}</td>
                <td>{{ postulant.getAnnonce[0].lieu }}</td>
                
                <td>{{ postulant.getAnnonce[0].description }}</td>
                <td>{{ postulant.valide ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_postulant_show', {'id': postulant.id}) }}">show</a>
                    <a href="{{ path('app_postulant_edit', {'id': postulant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_postulant_new') }}">Create new</a>
{% endblock %}
