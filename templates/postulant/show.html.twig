{% extends 'base.html.twig' %}

{% block title %}Postulant{% endblock %}

{% block body %}
    <h1>Postulant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ postulant.id }}</td>
            </tr>
            <tr>
                <th>Valide</th>
                <td>{{ postulant.valide ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_postulant_index') }}">Retour</a>

    <a href="{{ path('app_postulant_edit', {'id': postulant.id}) }}">Modifier</a>

    {{ include('postulant/_delete_form.html.twig') }}
{% endblock %}
